services:
  uploader:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3002:80
    volumes:
      - uploaded_files:/app/uploads
    networks:
      - web
volumes:
  uploaded_files:

networks:
  web:
    external: true